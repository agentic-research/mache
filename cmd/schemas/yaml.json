{
  "version": "v1",
  "nodes": [
    {
      "name": "mappings",
      "selector": "$",
      "children": [
        {
          "name": "{{.name}}",
          "selector": "(block_mapping_pair key: (flow_node) @name) @scope",
          "files": [
            {
              "name": "source",
              "content_template": "{{.scope}}"
            }
          ]
        }
      ]
    }
  ]
}

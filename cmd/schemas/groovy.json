{
  "version": "v1",
  "nodes": [
    {
      "name": "package",
      "selector": "$",
      "children": [
        {
          "name": "{{.index}}",
          "selector": "(package_declaration) @scope",
          "files": [
            {
              "name": "source",
              "content_template": "{{.scope}}"
            }
          ]
        }
      ]
    },
    {
      "name": "imports",
      "selector": "$",
      "children": [
        {
          "name": "{{.index}}",
          "selector": "(import_declaration) @scope",
          "files": [
            {
              "name": "source",
              "content_template": "{{.scope}}"
            }
          ]
        }
      ]
    },
    {
      "name": "classes",
      "selector": "$",
      "children": [
        {
          "name": "{{.name}}",
          "selector": "(class_declaration (identifier) @name) @scope",
          "files": [
            {
              "name": "source",
              "content_template": "{{.scope}}"
            }
          ]
        }
      ]
    },
    {
      "name": "functions",
      "selector": "$",
      "children": [
        {
          "name": "{{.name}}",
          "selector": "(function_definition (identifier) @name) @scope",
          "files": [
            {
              "name": "source",
              "content_template": "{{.scope}}"
            }
          ]
        },
        {
          "name": "{{.name}}",
          "selector": "(method_declaration (identifier) @name) @scope",
          "files": [
            {
              "name": "source",
              "content_template": "{{.scope}}"
            }
          ]
        }
      ]
    }
  ]
}
